<div class="comments">
  {{!-- comments array cant be destructured into an easily accessed variable name
  use this to refer to the partent object or array in the context of the partial --}}
  {{!-- Data that's passed to a template file is technically always part of this, but when dealing with objects, 
  ...we can simply access the object properties directly (e.g., vote_count instead of this.vote_count).
   ...Because arrays don't have named properties like objects do, we're forced to access the array as this. --}}
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{!-- format_date is a helpers util insert  --}}
      {{!-- This works essentially the same way as calling format_date(created_at) --}}
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>